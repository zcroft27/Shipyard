# This is a very small packages, it doesn't need a separate -dev package.
# Furthermore, in this package there are scripts calling the libeatmydata.so
# library directly.
libeatmydata1: link-to-shared-library-in-wrong-package
libeatmydata1: symbols-file-missing-build-depends-package-field
